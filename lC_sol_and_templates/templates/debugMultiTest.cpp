#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int solve(vector<int>& nums, int target) {
        return 0;
    }
};

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T = 1;
    while (T--) {
        vector<int> nums = {1, 2, 3};
        int target = 4;
        Solution sol;
        cout << sol.solve(nums, target) << "\n";
    }
}
